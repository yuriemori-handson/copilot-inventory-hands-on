# 在庫管理アプリケーション仕様書

## 1. 概要

本アプリケーションは、シンプルな在庫管理を行うための Web アプリケーションである。  
利用者は商品情報を登録・編集・削除し、各商品の在庫数を確認・更新できる。  
小規模な店舗やチーム内での利用を想定し、分かりやすく最小限の機能を提供する。

本アプリケーションはハンズオン用のサンプル実装を目的としており、複雑な業務要件や本番運用向けの高度な機能は含めない。

---

## 2. 想定技術構成

実装者は以下を前提としてアプリケーションを構築すること。

### バックエンド
- Python
- Web API フレームワークは FastAPI を利用すること

### フロントエンド
- React
- TypeScript
- 開発環境は Vite を利用すること

### データ保存
- 学習用アプリケーションのため、データはアプリケーションのメモリ上で保持すること
- 永続化は不要とする
- サーバー再起動時にはデータが初期化されてもよい

---

## 3. 利用者

本アプリケーションの利用者は、在庫情報を管理したい一般的な業務利用者を想定する。  
システム管理者向け機能や権限制御は不要とする。

---

## 4. 機能要件

### 4.1 商品一覧表示
利用者は登録済みの商品一覧を確認できること。

一覧には少なくとも以下の情報を表示すること。

- 商品ID
- 商品名
- SKU
- カテゴリ
- 価格
- 在庫数
- 在庫ステータス

在庫ステータスは以下のように表示すること。

- 在庫数が 0 の場合: 「在庫切れ」
- 在庫数が 1 以上 5 以下の場合: 「残りわずか」
- 在庫数が 6 以上の場合: 「在庫あり」

### 4.2 商品登録
利用者は新しい商品を登録できること。

登録時に入力できる項目は以下とする。

- 商品名
- SKU
- カテゴリ
- 価格
- 在庫数

### 4.3 商品編集
利用者は登録済み商品の情報を編集できること。

編集可能な項目は以下とする。

- 商品名
- SKU
- カテゴリ
- 価格
- 在庫数

### 4.4 商品削除
利用者は登録済みの商品を削除できること。

削除前には確認操作を挟むことが望ましい。

### 4.5 在庫数更新
利用者は商品一覧または編集画面から在庫数を更新できること。

在庫数更新は以下のいずれかの方法で実装すること。

- 在庫数そのものを直接編集する
- 増加・減少ボタンにより更新する

どちらを採用するかは実装時に決定してよい。

### 4.6 商品検索
利用者は商品名または SKU を条件に商品を検索できること。

検索は部分一致で行うこと。

---

## 5. 非機能要件

### 5.1 操作性
- 画面はシンプルで分かりやすい構成とすること
- PC ブラウザでの利用を前提とする
- 極端に複雑な UI は不要とする

### 5.2 保守性
- バックエンドとフロントエンドは役割を分離すること
- ソースコードは適切にファイル分割すること
- 主要処理は読みやすさを意識して実装すること

### 5.3 エラーハンドリング
- 必須項目が未入力の場合は、利用者に分かる形でエラー表示すること
- 不正な値が入力された場合は、適切にバリデーションすること
- API エラー発生時は、画面上で簡潔なメッセージを表示すること

---

## 6. データ要件

商品データは少なくとも以下の項目を持つこと。

- id: 一意な識別子
- name: 商品名
- sku: 在庫管理用のコード
- category: 商品カテゴリ
- price: 価格
- stock: 在庫数

### 6.1 項目制約
- 商品名は必須
- SKU は必須
- SKU は一意であること
- 価格は 0 以上の数値
- 在庫数は 0 以上の整数

---

## 7. API 要件

バックエンドは商品管理用の API を提供すること。  
最低限、以下の操作を行えること。

- 商品一覧取得
- 商品詳細取得
- 商品登録
- 商品更新
- 商品削除

必要に応じて検索機能を一覧取得 API に含めてもよい。

フロントエンドからの API 呼び出しは、開発時の接続トラブルを避けるため、同一オリジン経由（例: `/api` プレフィックスを Vite のプロキシでバックエンドに転送）を標準としてよい。
必要に応じて環境変数で API 接続先を上書きできるようにしてよい。

API の設計例は以下の通り。

- `GET /items`
- `GET /items/{id}`
- `POST /items`
- `PUT /items/{id}`
- `DELETE /items/{id}`

検索を行う場合の例:
- `GET /items?q=keyword`

実装時に多少の変更は許容するが、REST に近い分かりやすい構成とすること。

エラーレスポンスの例:
- 入力不正: `422 Unprocessable Entity`
- 存在しない商品 ID: `404 Not Found`
- SKU 重複: `409 Conflict`

---

## 8. 画面要件

最低限、以下の画面または UI 領域を持つこと。

### 8.1 商品一覧画面
- 商品一覧テーブルを表示する
- 商品検索欄を表示する
- 商品登録への導線を表示する
- 編集・削除操作を行えること

### 8.2 商品登録画面
- 商品登録フォームを表示する
- 登録成功後、一覧画面へ戻る、または成功メッセージを表示する

### 8.3 商品編集画面
- 既存データを初期表示したフォームを表示する
- 保存後、一覧画面へ戻る、または成功メッセージを表示する

画面数を減らすため、モーダルダイアログや同一画面内フォームで実装してもよい。

---

## 9. 初期データ

初回起動時、動作確認しやすいようにサンプル商品データをいくつか用意してもよい。

例:
- スキンケア用品
- シャンプー
- ボディソープ
- コスメ

初期データの有無は実装時に決定してよい。

---

## 10. 期待するふるまい（受け入れ条件）

本ハンズオンでは、以下の「利用者ができること」を満たすこと。

### 10.1 商品一覧を確認できること
- 利用者は、商品一覧画面で登録済みの商品を確認できること。
- 利用者は、各商品について ID・商品名・SKU・カテゴリ・価格・在庫数・在庫ステータスを確認できること。
- 受け入れ条件:
  - 一覧取得に成功した場合、商品がテーブル形式で表示されること。
  - 在庫数に応じて、在庫ステータスが「在庫切れ」「残りわずか」「在庫あり」として表示されること。

### 10.2 商品を登録できること
- 利用者は、商品登録フォームから新しい商品を登録できること。
- 受け入れ条件:
  - 必須項目を正しく入力して送信した場合、商品が登録され一覧に反映されること。
  - 登録後に、成功メッセージ表示または一覧への復帰が行われること。

### 10.3 商品を編集できること
- 利用者は、既存商品の情報を編集できること。
- 受け入れ条件:
  - 編集画面またはモーダルで既存値が初期表示されること。
  - 保存後、更新内容が一覧に反映されること。

### 10.4 商品を削除できること
- 利用者は、不要な商品を削除できること。
- 受け入れ条件:
  - 削除前に確認操作を行えること。
  - 削除後、対象商品が一覧に表示されないこと。

### 10.5 商品を検索できること
- 利用者は、商品名または SKU で商品を検索できること。
- 受け入れ条件:
  - 部分一致で検索できること。
  - 検索条件に一致する商品だけが一覧に表示されること。

### 10.6 入力エラーを理解できること
- 利用者は、入力不備や不正値がある場合に修正方法を理解できること。
- 受け入れ条件:
  - 必須未入力時にエラーが表示されること。
  - 価格が 0 未満、在庫数が負数または整数以外の場合にエラーが表示されること。
  - SKU 重複時にエラーが表示されること。

### 10.7 通信エラー時に継続操作できること
- 利用者は、API エラーや通信エラーが発生しても状況を把握し、再操作できること。
- 受け入れ条件:
  - エラー時に画面上へ簡潔なメッセージが表示されること。
  - 開発環境で商品登録時に通信経路の不整合（例: Failed to fetch）が発生しないこと。

---

## 11. 今回対象外とするもの

本ハンズオンでは、以下は対象外とする。

- ログイン認証
- 権限制御
- 高度なデザイン調整
- 画像アップロード
- CSV インポート / エクスポート
- バーコード読み取り
- 通知機能
- 本番環境へのデプロイ
- 高度な監査ログ
- 複数倉庫管理
- データ永続化

---

## 12. 実装時の補足

- ハンズオンでは、まず動作する最小構成を優先すること
- 過度に複雑な設計にはしないこと
- 読みやすく保守しやすいコードを目指すこと
- GitHub Copilot に実装計画、コード生成、説明、マニュアル作成を依頼しながら進めること

---

## 13. 完成イメージ

利用者は以下のような流れで操作できること。

1. 商品一覧画面を開く
2. 登録済み商品の在庫状況を確認する
3. 必要に応じて商品を検索する
4. 新しい商品を登録する
5. 既存商品の価格や在庫数を編集する
6. 不要な商品を削除する

以上を通して、シンプルな在庫管理業務を実行できること。
